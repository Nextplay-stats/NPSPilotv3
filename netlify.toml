[build]
  # your static site’s publish folder (e.g. "public", "dist", or "." if you have no build step)
  publish   = "."    
  # point Netlify to your functions dir
  functions = "functions"
  command   = "npm run build"

[dev]
  functions = "functions"  # for `netlify dev`
  port      = 8888
